<div id="gestion">
  <h2>Administration</h2>
  <p><a href="#">Gérer les vin</a></p>
  <p><a href="#"></a></p>
  <p><a href="#">Gérer les commandes</a></p>
  <p><%= link_to 'Gérer évènement', events_path%></p>
</div>

<div id="conteneur_intermediaire_right">
  <div class="article">
    <h1>Liste des évènements</h1>
    <br/>
    <table>
      <thead>
        <tr>
          <th>Titre</th>
          <th>Date début</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @events.each do |event| %>
          <tr>
            <td><%= event.title %></td>
            <td><%= event.beginAt %></td>
            <td><%= link_to 'Afficher', event %></td>
            <td><%= link_to 'Editer', edit_event_path(event) %></td>
            <td><%= link_to 'Supprimer', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br/>
    <%= link_to 'Ajouter un nouvel évènement', new_event_path %> 
  </div>
</div>

<!--
<p id="notice"><%= notice %></p>

<h1>Listing Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Text</th>
      <th>Beginat</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.text %></td>
        <td><%= event.beginAt %></td>
        <td><%= event.image %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
-->
